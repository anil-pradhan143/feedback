apiVersion: v1
kind: ConfigMap
metadata:
  name: carstaxi-feedback-voc
data:
  appConfig.js: |
    const appConfig = {
      NODE_ENV: 'dev',
      BASE_URL: 'https://buddysdev.agp-dev.com/global/voc'
    };